# .github/workflows/test.yml
name: tests

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "master"

jobs:
  phpunit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build image
        run: docker build -f docker/Dockerfile -t pictshare-test .

      - name: Install dev dependencies (PHPUnit)
        run: |
          docker run --rm \
            --entrypoint bash \
            -v "${{ github.workspace }}:/app/public" \
            pictshare-test \
            -c "cd /app/public/src/lib && composer install --no-interaction"

      - name: Run PHPUnit
        run: |
          docker run --rm \
            --entrypoint php \
            -v "${{ github.workspace }}:/app/public" \
            pictshare-test \
            /app/public/src/lib/vendor/bin/phpunit \
              --configuration /app/public/phpunit.xml \
              --colors=never
